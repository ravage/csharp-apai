## Opening the Projects
They should seemingly work in Visual Studio or Visual Studio Code. For running
tests inside Visual Studio Code the plugin *.NET Core Test Explorer* may help.

## Running the TestCases Command Line
* Go into <Something>Test directory
* Run `dotnet test`
* Or `dotnet watch test`
